@model RoomModel.Src.Dto.InvoiceDto
@{
    Layout = null;
    ViewBag.Title = "Create";
}

<form asp-action="Create" asp-controller="Invoice" method="post" id="messageModal" class="frmValidation" novalidate>
    @Html.AntiForgeryToken()
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">New Invoice</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        </div>
        <div class="modal-body">
            <div class="col-lg-12">
                <div class="row">
                    <div class="col-lg-6">
                        <label for="InvoiceId">InvoiceId<span style="color:red">*</span></label><br />
                        <input type="text" asp-for="InvoiceId" class="form-control" id="InvoiceId" autocomplete="off" required />
                        <div class="invalid-feedback">
                            Please Enter A InvoiceId.
                        </div>
                    </div>
                    <div class="col-lg-6">
                        <label for="ToDate">Title<span style="color:red">*</span></label><br />
                        <input type="text" asp-for="ReservationId" class="form-control" id="Title" autocomplete="off" required />
                        <div class="invalid-feedback">
                            Please Enter A To Title.
                        </div>
                    </div>
                </div>
            </div><div class="col-lg-12">
                <div class="row">
                    <div class="col-lg-6">
                        <label for="RoomCharge">RoomCharge<span style="color:red">*</span></label><br />
                        <input type="text" asp-for="RoomCharge" class="form-control" id="RoomCharge" autocomplete="off" required />
                        <div class="invalid-feedback">
                            Please Enter A RoomCharge.
                        </div>
                    </div>
                    <div class="col-lg-6">
                        <label for="ToDate">Title<span style="color:red">*</span></label><br />
                        <input type="text" asp-for="AdditionalCharges" class="form-control" id="Title" autocomplete="off" required />
                        <div class="invalid-feedback">
                            Please Enter A To Title.
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-lg-12">
                <div class="row">
                    <div class="col-lg-6">
                        <label for="TaxAmount">TaxAmount<span style="color:red">*</span></label><br />
                        <input type="text" asp-for="TaxAmount" class="form-control" id="TaxAmount" autocomplete="off" required />
                        <div class="invalid-feedback">
                            Please Enter A Servicecharge.
                        </div>
                    </div>

                </div>
            </div>
            <div class="col-lg-12">
                <div class="row">
                    <div class="col-lg-6">
                        <label for="TotalAmount">TotalAmount<span style="color:red">*</span></label><br />
                        <input type="text" asp-for="TotalAmount" class="form-control" id="TotalAmount" autocomplete="off" required />
                        <div class="invalid-feedback">
                            Please Enter A Servicecharge.
                        </div>
                    </div>

                </div>
            </div>

        </div>
        <div class="modal-footer">
            <button type="submit" id="btnSubmit" class="btn  btn-success"><i class="feather icon-check-square"></i> Submit</button>
            <button type="button" class="btn  btn-secondary" data-dismiss="modal">&times; Cancel</button>
        </div>
    </div>
</form>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
@if (ViewBag.Message != null)
{
    <script type="text/javascript">
    'use strict';
    $(window).on('load', function () {
        function notify(message) {
            $.notify({
                message: message,
                type: 'danger',
                allow_dismiss: false,
                label: 'Cancel',
                className: 'btn-xs btn-danger',
                placement: { from: 'bottom', align: 'right' },
                delay: 2500,
                animate: { enter: 'animated fadeInRight', exit: 'animated fadeOutRight' },
                offset: { x: 30, y: 30 }
            });
        };
        /**/
        notify('@ViewBag.Message');
    /**/
    });
    </script>
}


<script type="text/javascript">
    $(function () {
        Array.prototype.filter.call($('.frmValidation'), function (form) {
            $('#btnSubmit').click(function () {
                if (form.checkValidity() === false) {
                    event.preventDefault();
                    event.stopPropagation();
                    form.classList.add('was-validated');
                    return false;
                }
                else {
                    return true;
                }


            });
        });
    });

</script>



